<Page
    x:Class="WeGo.Weather"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WeGo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical" Margin="0 50">
            <Image x:ConnectionId='1' Name="WeatherIcon" Width="100" Height="100"></Image>
            <TextBlock x:ConnectionId='2' Name="City"></TextBlock>
            <TextBlock x:ConnectionId='3' Name="Cond"></TextBlock>
            <TextBlock x:ConnectionId='4' Name="Tem"></TextBlock>
            <TextBlock x:ConnectionId='5' Name="Aqi"></TextBlock>
            <TextBlock x:ConnectionId='6' Name="More">正在获取温度中请稍后</TextBlock>
            <AutoSuggestBox x:ConnectionId='7' Width="200" QueryIcon="Find"
                            Name="WeatherCityList" PlaceholderText="搜索"
                                                                     
                                                                           
                            TextMemberPath="id">
                <AutoSuggestBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock FontFamily="微软雅黑">
                            <Run Text="{Binding cityZh}"/>
                            <Run Text=","></Run>
                            <Run Text="{Binding leaderZh}"/>
                            <Run Text=","></Run>
                            <Run Text="{Binding provinceZh}"/>
                        </TextBlock>
                    </DataTemplate>
                </AutoSuggestBox.ItemTemplate>
            </AutoSuggestBox>
        </StackPanel>
        
    </Grid>
</Page>

